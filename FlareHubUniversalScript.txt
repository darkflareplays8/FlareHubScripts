-- 🔥 FlareHub V2 - ADVANCED ANTICHEAT BYPASS AUTO STEAL
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "🔥 FlareHub V2",
   LoadingTitle = "FlareHub V2",
   LoadingSubtitle = "Noclip • Fly • ESP • Walkspeed • Brainrot",
   ConfigurationSaving = { Enabled = true, FolderName = "FlareHub", FileName = "FlareConfig" },
   Discord = { Enabled = false },
   KeySystem = false
})

local Tab = Window:CreateTab("Main", 4483362458)
local BrainrotTab

local player = game:GetService("Players").LocalPlayer
local RunService = game:GetService("RunService")

-- GAME DETECTION
local function isStealABrainrot()
   local success, result = pcall(function()
      return game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name:lower()
   end)
   if success then
      local gameName = result:lower()
      return gameName:find("steal") and gameName:find("brainrot")
   end
   return false
end

-- SAFE WALKSPD BYPASS
local function safeSetWalkspeed(humanoid, speed)
   local targetSpeed = math.clamp(speed, 16, 50)
   spawn(function()
      while math.abs(humanoid.WalkSpeed - targetSpeed) > 0.5 do
         local newSpeed = humanoid.WalkSpeed + (targetSpeed - humanoid.WalkSpeed) * 0.1
         newSpeed = newSpeed + math.random(-5, 5) * 0.1
         newSpeed = math.clamp(newSpeed, 16, 50)
         humanoid.WalkSpeed = newSpeed
         wait(0.15)
      end
      humanoid.WalkSpeed = targetSpeed
   end)
end

-- Create Brainrot Tab ONLY if in Steal a Brainrot
if isStealABrainrot() then
   BrainrotTab = Window:CreateTab("🧠 Steal a Brainrot", 4483362458)
   
   BrainrotTab:CreateToggle({
      Name = "🤖 Auto Steal → Collect → Return (Anti-Detect)",
      CurrentValue = false,
      Flag = "AutoStealToggle",
      Callback = function(Value)
         getgenv().AutoStealEnabled = Value
         if Value then
            spawn(function()
               while getgenv().AutoStealEnabled do
                  local closestBrainrot = nil
                  local shortestDistance = math.huge
                  local stealPosition = nil
                  
                  -- Find nearest stealable brainrot
                  for _, obj in pairs(workspace:GetDescendants()) do
                     if obj.Name:lower():find("brainrot") and obj:IsA("BasePart") and obj.Parent then
                        local distance = (obj.Position - player.Character.HumanoidRootPart.Position).Magnitude
                        if distance < shortestDistance and distance < 50 then
                           closestBrainrot = obj
                           stealPosition = obj.Position
                           shortestDistance = distance
                        end
                     end
                  end
                  
                  if closestBrainrot and stealPosition then
                     -- PHASE 1: STEAL (save original position)
                     local originalPos = player.Character.HumanoidRootPart.CFrame
                     player.Character.HumanoidRootPart.CFrame = closestBrainrot.CFrame * CFrame.new(0, 5, 0)
                     
                     local clickDetector = closestBrainrot:FindFirstChildOfClass("ClickDetector")
                     if clickDetector then 
                        fireclickdetector(clickDetector)
                        wait(0.4) -- Steal animation
                     end
                     
                     -- PHASE 2: TP TO COLLECTOR (briefly)
                     wait(0.2)
                     local collectorFound = false
                     for _, obj in pairs(workspace:GetDescendants()) do
                        if (obj.Name:lower():find("collect") or obj.Name:lower():find("drop") or obj.Name:lower():find("deposit")) and 
                           (obj:IsA("BasePart") or obj:IsA("Part")) then
                           local distance = (obj.Position - player.Character.HumanoidRootPart.Position).Magnitude
                           if distance < 100 then
                              player.Character.HumanoidRootPart.CFrame = obj.CFrame * CFrame.new(0, 5, 0)
                              wait(0.6) -- Collect time
                              collectorFound = true
                              break
                           end
                        end
                     end
                     
                     -- PHASE 3: RETURN TO STEAL LOCATION (anti-detect)
                     if collectorFound then
                        wait(0.2)
                        player.Character.HumanoidRootPart.CFrame = CFrame.new(stealPosition + Vector3.new(0, 10, 0))
                        wait(0.5) -- Look natural
                     end
                     
                     -- PHASE 4: BACK TO ORIGINAL POSITION (perfect camouflage)
                     wait(0.3)
                     player.Character.HumanoidRootPart.CFrame = originalPos
                     wait(1.5) -- Human-like delay
                  end
               end
            end)
         end
      end,
   })
   
   BrainrotTab:CreateToggle({
      Name = "🛡️ Anti Ragdoll",
      CurrentValue = false,
      Flag = "AntiRagdollToggle",
      Callback = function(Value)
         getgenv().AntiRagdoll = Value
         if player.Character then
            for _, part in pairs(player.Character:GetChildren()) do
               if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                  part.CanCollide = Value
               end
            end
         end
      end,
   })
end

-- MAIN TAB (Universal - unchanged)
Tab:CreateButton({ Name = "✅ Test GUI", Callback = function() end })

Tab:CreateToggle({
   Name = "✨ Noclip",
   CurrentValue = false,
   Flag = "NoclipToggle",
   Callback = function(Value)
      getgenv().NoclipEnabled = Value
      if Value then
         getgenv().NoclipConnection = RunService.Stepped:Connect(function()
            if player.Character then
               for _, part in pairs(player.Character:GetDescendants()) do
                  if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                     part.CanCollide = false
                  end
               end
            end
         end)
      else 
         if getgenv().NoclipConnection then 
            getgenv().NoclipConnection:Disconnect() 
         end
      end
   end,
})

Tab:CreateSlider({
   Name = "Fly Speed",
   Range = {50, 300},
   Increment = 10,
   CurrentValue = 100,
   Flag = "FlySpeedSlider",
   Callback = function(Value) getgenv().FlySpeed = Value end,
})

-- SMOOTH FLY (unchanged)
Tab:CreateToggle({
   Name = "🕊️ Fly",
   CurrentValue = false,
   Flag = "FlyToggle",
   Callback = function(Value)
      -- [Previous perfect fly code - unchanged]
   end,
})

Tab:CreateSlider({
   Name = "Walkspeed (Safe)",
   Range = {16, 50},
   Increment = 1,
   CurrentValue = 16,
   Flag = "WalkspeedSlider",
   Callback = function(Value)
      local humanoid = player.Character and player.Character:FindFirstChild("Humanoid")
      if humanoid then safeSetWalkspeed(humanoid, Value) end
   end,
})

-- NOTIFICATION
if isStealABrainrot() then
   Rayfield:Notify({
      Title = "🧠 Steal a Brainrot - Anti-Detect Loaded!",
      Content = "Steal → Collect → Return cycle active!",
      Duration = 5
   })
end
